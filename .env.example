# Copy this file to .env and update with your values

# Backend URL (server-side only, used by Next.js API proxy)
# This is where the WhatsApp API backend is running
# Default: http://localhost:3008
BACKEND_URL=http://localhost:3008

# WebSocket port for real-time updates (client-side)
# The frontend will connect to ws://<current-hostname>:<port>
# Default: 3008
NEXT_PUBLIC_WS_PORT=3008

# Optional: Full WebSocket URL override (if you need a custom URL)
# NEXT_PUBLIC_WS_URL=ws://custom-host:3008
